<section class="card">
    <div class="row ">
        <div class="col-md-8">
            <h4><a href="/project/{{project.id}}">{{project.title}}</a></h4>
            <p>{{project.description}}</p> 
      </div>
    </div>
</section>